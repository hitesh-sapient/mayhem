
<br>

<div class="row columns">
  <nav aria-label="You are here:" role="navigation">
    <ul class="breadcrumbs">
      <li><a href="#">Home</a></li>
      <li><a href="#">Women</a></li>
      <li class="disabled">Dress</li>
      <li>
      <span class="show-for-sr">Current: </span> <%= requestData.name %>
      </li>
    </ul>
  </nav>
</div>
<div class="row">
  <div class="medium-6 columns">
  <img class="thumbnail" src="<%= requestData.images[0].normal %>">
  <div class="row small-up-4">
    <% for(var i=1; i<requestData.images.length; i++) {%>
      <div class="column">
        <img class="thumbnail" src=<%= requestData.images[i].small %>>
      </div>
    <% } %>
  </div>
  </div>
  <div class="medium-6 large-5 columns">
    <h3><%= requestData.name %></h3>
    <p>£<%= requestData.priceSummary.minimum %></p>
    <p><%= requestData.description %></p>
    <label>Colour
      <select>
        <% for(var i=0; i<requestData.styles.length; i++) {%>
          <option value="<%= requestData.styles[i].colour %>"><%= requestData.styles[i].colour %></option>
        <% } %>
      </select>
    </label>
    <div class="row">
      <div class="small-3 columns">
        <label for="middle-label" class="middle">Quantity</label>
      </div>
      <div class="small-9 columns">
        <input type="text" id="middle-label" placeholder="qualtity">
      </div>
    </div>
    <a href="#" class="button large expanded">Add to Bag</a>
    <div class="small secondary expanded button-group">
    </div>
  </div>
</div>
<div class="column row">
  <hr>
  <ul class="tabs" data-tabs id="example-tabs">
    <li class="tabs-title is-active"><a href="#panel1" aria-selected="true">Reviews</a></li>
  </ul>
  <div class="tabs-content" data-tabs-content="example-tabs">
    <div class="tabs-panel is-active" id="panel1">
      <h4>Reviews</h4>
      <div class="media-object stack-for-small">
        <div class="media-object-section">
          Rating : <%= requestData.reviewsSummary.averageRating %>
        </div>
      <div class="media-object-section">
          Review Count : <%= requestData.reviewsSummary.reviewCount %>
      </div>
    </div>
    </div>
  </div>
</div>
